package checker

import "github.com/7joe7/hamrchecker/resources"

func RunSearch(search *resources.Search) {
	runSearch(search)
}

func SetEmailConfiguration(emailConf *resources.EmailConf) {
	setEmailConfiguration(emailConf)
}
